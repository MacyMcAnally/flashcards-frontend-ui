import React from 'react'
import Header from '../Header';

function UserInfo() {
    return (
        <>
            <Header />
        <h1>This is UserInfo Page!</h1>
        </>
        //this page will allow the user to see their username, user_id, password(hashed)
        //be uable to update user infro and delete user? (is this necessary?)
    )
}

export default UserInfo;
